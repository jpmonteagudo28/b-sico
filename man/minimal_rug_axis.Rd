% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis.R
\name{minimal_rug_axis}
\alias{minimal_rug_axis}
\title{Draw Minimal Rug Axis with Selective Labeling}
\usage{
minimal_rug_axis(
  .x,
  sides = "bottom",
  at = NULL,
  line_width = 0,
  tick_width = 0.7,
  tick_length = 0.5,
  tick_color = "black",
  mean_color = "red",
  label_summ_stats = FALSE,
  label_gap = 1.5,
  digits = 2,
  ...
)
}
\arguments{
\item{.x}{A numeric vector. The data used for the axis ticks and labels.}

\item{at}{Optional. A numeric vector specifying the exact positions for ticks.
If `NULL`, the function uses the values from `.x`.}

\item{line_width}{Numeric. The width of the axis line. Default is `0` (no line).}

\item{tick_width}{Numeric. The width of the tick marks. Default is `0.7`.}

\item{tick_length}{Numeric. The length of the tick marks as a fraction of the plot dimension.
Default is `0.5`.}

\item{tick_color}{Character. The color of the tick marks. Default is `"black"`.}

\item{mean_color}{Character. The color used to highlight the mean value label. Default is `"red"`.
Default is `NULL`, meaning labels are determined dynamically.}

\item{label_summ_stats}{Logical. If `TRUE`, only summary statistics (`min`, `Q1`, `median`,
`mean`, `Q3`, `max`) will be labeled. If `FALSE`, labels will be determined based on spacing.
Default is `FALSE`.}

\item{label_gap}{Numeric. The minimum spacing between labels to prevent overlap.
Expressed as a multiplier of the median spacing between values. Default is `1.5`.}

\item{digits}{Integer. The number of decimal places to round the labels. Default is `2`.}

\item{...}{Additional graphical parameters passed to the `axis()` function.}

\item{side}{A character vector indicating which sides of the plot to draw the axis on.
Options are `"bottom"`, `"left"`, `"top"`, and `"right"`. Default is `"bottom"`.}
}
\value{
The function is used for its side effects (drawing on the existing plot). It does not
return a value.
}
\description{
This function adds a minimalist axis with rug ticks and selective labeling to a plot.
It allows for customized control over tick marks, tick lengths, label spacing, and
highlighting of key summary statistics or specific values, such as the mean.
}
\details{
The function draws rug ticks on specified sides of the plot using the values from `.x`
or the positions defined in `at`. It includes options for drawing only specific labels
(like summary statistics) or dynamically adjusting the labels to avoid overcrowding.

If `label_summ_stats = TRUE`, the function will label only the minimum, first quartile,
median, mean, third quartile, and maximum values. Otherwise, it will label values
based on spacing criteria defined by `label_gap`.

The function uses `mtext()` to add labels and `axis()` to draw ticks without a full axis line,
allowing for a clean, minimalist appearance similar to Edward Tufte's style.
}
\examples{
set_font("Alegreya")
plot.new()
plot.window(range(mtcars$wt),range(mtcars$mpg))
points(mtcars$wt,mtcars$mpg, pch = 21, col = "black")
minimal_rug_axis(mtcars$wt, sides = "bottom", tick_length = 0.75, label_summ_stats = TRUE)
minimal_rug_axis(mtcars$mpg, sides = "left", label_summ_stats = TRUE)
mtext(
  "Car weight (lb/1000) v. \nMiles per gallon",
  side = 3,
  at = 4.55,
  line = -4.5,
  adj = 0,
  cex = 1.2,
  col = "black",
  font = 3
)

}
\seealso{
\code{\link{axis}}, \code{\link{mtext}}, \code{\link{rug}}
}
