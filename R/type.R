# Add jitter to plots
type_jitter <- function(factor = 1,
                       amount = NULL){

  out = list(
    draw =,
    data = ,
    name = "p"

  )

  class(out) = "chart_type"
  return(out)
}

data_jitter = function(factor, amount){



}
